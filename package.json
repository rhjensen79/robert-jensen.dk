{
  "name": "robert-jensen-dk",
  "version": "1.0.0",
  "private": true,
  "description": "Tooling for the robert-jensen.dk Hugo site (Pagefind search index).",
  "scripts": {
    "search:index": "pagefind --site public",
    "search:serve": "pagefind --site public --serve",
    "build:search": "hugo --minify && pagefind --site public"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
